# IDEAS — Аффирмации и поддержка (`affirmations.md`)

**Status**

- Уже реализовано:
  - Настройки аффирмаций в wellness: режимы `off / morning / evening / both`.
  - Выборочная вставка мысли дня в `/today` через `utils.affirmations.random_affirmation_text`.
  - Небольшие поддерживающие фразы в статистике и щадящем режиме (`utils.texts.gentle_*`).
- Next sprint candidates:
  - Расширить набор аффирмаций (20–50 штук) с разбивкой по категориям.
  - Тонко подбирать формулировки под состояние пользователя (стресс, выгорание, «ничего не делаю»).

---

## 1. Категории аффирмаций

Базовые категории:

- Самооценка и право на ошибку.
- Мотивация маленьких шагов.
- ADHD‑friendly / «мозг‑обезьяна» (про переключение и забывчивость).
- Дом/уборка: «ты не свалка, просто был сложный период».
- Здоровье/забота о себе (сон, еда, вода).

`Done`:

- [x] Категории отражены в данных явно: используется поле `category` (`self_worth`, `motivation`, `adhd`, `rest`, `anxiety` и т.п.).
- [x] В `data/affirmations.json` хранится структура `{id, category, tone, text}`.
- [x] В `utils/affirmations.py` есть хелперы `random_affirmation` / `random_affirmation_text`, которые выбирают текст по категории.

`Next sprint candidates`:

- [ ] Тонко подбирать категории под контекст (стресс, выгорание, «щадящий день», успехи по дому).
- [ ] Добавить дополнительные категории при необходимости (например, `home`, `health`), не ломая существующие.

---

## 2. Источники и хранение текстов

Сейчас:

- набор мыслей хранится в `data/affirmations.json`;
- выбор происходит через `utils/affirmations.random_affirmation_text`, пока без учёта контекста.

`Next sprint candidates`:

- [ ] Добавить в `docs/dev_notes.md` короткую памятку, как безопасно дополнять набор аффирмаций (какие поля есть, как выбирать категории).

---

## 3. Где именно используются аффирмации

Уже есть:

- `/today` — строка «💬 Мысль дня: …» по настройкам (`morning/evening/both`).
- `/stats` — мягкие фразы про стрик через `gentle_streak`.
- Щадящий режим — включение `gentle` даёт более мягкие тексты и меньше задач.

`Next sprint candidates`:

- [ ] Добавить аффирмации в конце тяжёлых сценариев (уборка сейчас, большой день, фокус 20/10).
- [ ] Дать 1–2 коротких текста «самопрощения» после серий пропусков (когда пользователь сам жмёт щадящий режим).

---

## 4. Done

- [x] Настройки аффирмаций (`affirm_mode`) в wellness.
- [x] Отображение мысли дня в `/today`.
- [x] Базовый набор поддерживающих фраз в `utils.texts` и статистике.
