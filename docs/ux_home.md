# UX-спека: Дом и уборка

## Цель
Домашний раздел должен помогать быстро выбрать подходящее действие: план на неделю, все дела, или “уборка сейчас” под текущее состояние сил. Без перегруза кнопками, с мягкими формулировками.

## Первый экран “Дом”
Текст: “Дом: помогу навести порядок по-человечески. Выбирай, что актуально: быстренько убраться, посмотреть план на неделю или разобраться с регулярными делами.”

Кнопки (inline):
- 🧽 Уборка сейчас (`home:now`)
- 📅 План на неделю (`home:week`)
- 📋 Все дела по дому (`home:all`)
- 🧴 Запахи/стирка (`home:smell`) → ведёт в “Спросить маму” (стирка/запахи).

## План на неделю
- Список 2–5 ближайших регулярных задач: “• До DD.MM.YYYY — {title}”
- Кнопки на каждую задачу: “✅ {title}” | “⏭ +1” | “+3” | “+7”
- После отметки/отложки — обновить список без спама сводкой дня.
- Если пусто: “План по дому на неделю пуст — всё чисто.”

## Все дела по дому
- Список всех активных регулярных задач с частотой: “⏳ Полы — каждые 7 д., до 12.12.2025”.
- Кнопки по строке:
  - ✅ (done)
  - ⚙️ Частота (показать пресеты 7/14/30/90 + “Своя”)
  - 🗑 (скрыть/деактивировать)
- После действия — обновить список.

## Уборка сейчас
### Шаги диалога
1) Выбор типа:
   - ✨ Быстрый порядок (surface)
   - 🧹 Нормальная уборка (normal)
   - 🧽 Одна зона поглубже (deep)
2) Выбор сил:
   - Почти не живая
   - Могу нормально
   - Готова поработать
3) Генерация 3–7 шагов:
   - Surface: лёгкие визуальные шаги (собрать одежду, протереть стол, замочить посуду, вынести мусор).
   - Normal: 1–2 ближайших regular tasks (“по плану”) + 1–3 лёгких шагов.
   - Deep (зона): выбрать зону (кухня/ванна/комната/прихожая) и дать 3–5 шагов внутри зоны.
4) Кнопки на каждый шаг:
   - ✅ Готово
   - ⏭ Пропустить
5) Завершение:
   - Если все шаги отмечены — “Ты закрыла N из M шагов, +X очков. Дома уже легче — можешь остановиться или сделать ещё один круг позже.” + main_menu.
   - Не спамить сводкой /today автоматически.

### Правила
- Конвертировать sqlite3.Row → dict, чтобы не падать.
- Очки: 1–2 за микро, 3–5 за большие или “по плану”.
- Поддерживать повторный запуск: state хранит шаги/статус/день.

## Регулярка в /today
- Показывать только 2–3 ближайших дела, без кнопок +1/+3/+7 (они в отдельном экране).
- Текст: “🔁 Регулярка по дому: • Полы — до DD.MM.YYYY …”
- Если пусто — не показывать блок.

## Запахи/стирка
- Кнопка `home:smell` → в /ask_mom на категорию “запахи/стирка”.
- Короткий ответ: “Запах/стирка: выбери тему” + инлайн-кнопки темы.

## Тональность
- Женский, тёплый, без стыда: “если нет сил — сделай 1 шаг”.
- Никаких команд в тексте (вместо “/home” — кнопки).
- Даты в DD.MM.YYYY, время HH:MM локальное.

## Примеры
### План на неделю (пример)
```
План по дому на ближайшие 7 дней:
• До 12.12.2025 — Полы/пылесос
• До 13.12.2025 — Ванна/раковина
```
Клавиатура: строки с ✅, ⏭ +1, +3, +7.

### Уборка сейчас (surface, mid)
```
Сделай эти шаги:
• Собери одежду в одну корзину
• Протри стол/поверхность
• Замочи посуду в раковине
• Вынеси мусор, если ведро полное
```
Кнопки на каждый: ✅ / Пропустить. После финала — отчёт с очками.

## Связанные файлы
- `handlers/home_tasks.py` — основной сценарий дом/уборка/регулярка.
- `handlers/menu.py` — входные кнопки “Дом”, callbacks.
- `utils/today.py` — отображение регулярки в /today.
- `utils/texts.py`, `utils/tone.py` — тон/унифицированные тексты.
