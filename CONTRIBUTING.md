# Как вносить изменения в HiDL

1. Перед началом
   - Прочитай `README.md`, `docs/architecture.md` и актуальный спринт (`sprints/sprint3.md` или новее).
   - Не добавляй новые команды без UX-спек (смотри `docs/ux_*`).
   - Работай в своей ветке, не коммить секреты (`.env` и токены).

2. Запуск
   - `python -m venv .venv && source .venv/bin/activate`
   - `pip install -r requirements.txt`
   - `cp .env.example .env` и впиши `BOT_TOKEN`.
   - `python main.py` или `scripts/run_dev.sh`.

3. Перед пушем
   - `python -m tests.smoke`
   - Ручной маршрут: `/start → /today → Дом → Уборка сейчас → План по дому → Напоминания → Деньги → Мои очки`.
   - Убедись, что даты/время в ответах в формате `DD.MM.YYYY` и `HH:MM`.

4. Стиль кода
   - Простые функции, говорящие имена, docstring по делу.
   - Не шеймить пользователя: тексты мягкие, в женском роде.
   - Все `sqlite3.Row` приводим к dict через `utils.rows.row_to_dict`.

5. Документация
   - Если меняешь UX или команды — обнови соответствующий `docs/ux_*.md` и спринт.
   - Коротко опиши изменения в `docs/changelog_sprint3.md` (или следующем changelog).

Спасибо, что помогаешь HiDL быть заботливой! 😊
