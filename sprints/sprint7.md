# Sprint 7 — Симулятор, Dogfooding и UX-полировка

**Цель:** тестировать бота через симулятор, быстро ловить баги и делать UX удобнее.

## Итоги тестирования
**Дата:** 2025-12-16  
**Статус:** batch_test: 24/24 сценария прошли

---

## Исправлено / улучшено

### FIX-001: Inline кнопки не работали в `tools/batch_test.py`
- Тест начал отправлять `CallbackQuery` для inline-кнопок.

### FIX-002: `"08:00"` парсилась как трата
- `parse_expense()` перестал матчить время как трату.

### FIX-003: Категория траты определялась неправильно
- `"потратил 500 на еду"` → категория `"еду"`, а не `"на"`.

### FIX-004: `"купила молоко и хлеб"` некорректно парсилось
- Убраны артефакты типа `"а молоко"`, улучшен разбор.

### FIX-005: APScheduler падал на `TelegramForbiddenError` (bot→bot / blocked)
- Все рассылки в `scheduler/reminder.py` теперь идут через safe-send; ошибка не валит job.
- При `Forbidden: bots can't send messages to bots` пользователь ставится на долгую паузу, чтобы не спамить лог.

### UX-001: Добавлены кнопки `⬅️ Назад` там, где пользователь “застревал”
- Подменю теперь возвращают в главное меню; в “Дом” добавлены явные “назад” в списках/пагинации.

### UX-002: Кнопка `💚 Спасибо` в аффирмациях стала “живой”
- На `Спасибо` бот отвечает мягким toast-ответом, без засорения чата.

### UX-003: /today — строка “Дом” стала информативнее
- В сводке `/today` показываются 1–2 названия ближайших дел по дому (в ADHD режиме — 1) + корректные склонения.

### UX-004: Рутины и кнопки — более заботливые и без “(а)”
- Обновлены базовые рутины Утро/День/Вечер (больше пользы, меньше мусора).
- Кнопки подтверждения теперь без “Сделал(а)”: учитывают пол (или показывают нейтральное “Готово”).
- “Движение” переименовано в “Спорт” в меню и сценариях симулятора.

### CONTENT-001: Рутины — убраны нелогичные пункты
- В утренней рутине заменён странный пункт про “чистые вещи” на более полезный чек.

### STATS-001: “Очки за 7 дней” → “за неделю” (с понедельника)
- Статистика и /today показывают очки за текущую неделю, а не “плавающие 7 дней”.

### UX-005: Кафе фокуса (интерактивная сессия)
- /cafe и кнопка “☕ Кафе фокуса” в меню.
- Сессии с чек‑ином в середине и итогом в конце, очки за результат.
- За серию срывов включается пауза по фокусу.

### UX-006: Тихий режим уведомлений
- Переключатель в настройках уведомлений.
- Отключает системные пинги (рутины/план/аффирмации/таблетки и т.д.), оставляет только свои напоминания.
- В /today появляется заметка про тихий режим.

### BUG-006: Парсер трат не мешает вводам в FSM
- Ввод количества продуктов больше не улетает в “трату” при добавлении в кладовку/покупки.
- Парсинг “естественного” текста не срабатывает, когда активен FSM.

### TEST-002: Симулятор обновлён под текущий UI
- Обновлены сценарии под актуальные кнопки.
- Добавлены сценарии `focus_cafe` и `quiet_mode`.

### UX-007: Кнопки «Назад» в ключевых меню
- Напоминания, Деньги и Таблетки получили явный возврат в меню.

---

## Известные проблемы (TODO)

### BUG-003: Напоминание “через 3 дня” (проверка)
- Нужно перепроверить на живом потоке и/или добавить отдельный сценарий в симулятор.

---

## Next
- [ ] Ручной UX-догофудинг “Кафе фокуса” + корректировки (тон, очки, тайминги)
- [x] Прогон симулятора с новыми сценариями, фиксация находок в `IDEAS/ux_findings_sprint7.md`
- [ ] Подготовка и деплой на Railway (env vars, smoke после деплоя)

---

## Ключевые файлы
- `scheduler/reminder.py` — safe-send + защита от падений scheduler jobs
- `keyboards/common.py` — “⬅️ Назад” в меню
- `handlers/affirmations.py` — “Спасибо” в аффирмациях
- `utils/today.py`, `handlers/stats.py`, `db/repositories.py` — “за неделю” и недельная статистика
