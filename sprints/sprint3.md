# Sprint 3 — HiDL: стабильность, скелет и «мамин» комфорт

Цели спринта:

1) Сделать ядро HiDL стабильным: ни одна основная кнопка не должна ронять бота.  
2) Дотянуть UX по дому/уборке, напоминаниям, /today и деньгам до состояния «открыл — понял — нажал».  
3) Доделать женский тон, анти-стыд и единый стиль текстов.  
4) Подготовить архитектурные и UX-спеки, чтобы Codex понимал проект, а не гадал.  
5) Заложить фундамент под естественные команды и будущий LLM-чат-режим.

> Для Codex: отмечай выполненное `[x]`.  
> Тексты — по-русски, от лица HiDL (женский род, тёплый тон, без шейминга).

---

## 0. Общие правила спринта

- [ ] Все задачи спринта описаны только здесь (`sprints/sprint3.md`). Если что-то придумывается по ходу — добавляем сюда отдельным пунктом.
- [ ] После каждого крупного блока — запуск дымового теста (см. раздел 7).
- [ ] Никаких новых команд без обновления /help и UX-спек.

---

## 1. Документация архитектуры и UX (карта для Codex)

**Задача: чтобы ИИ-разработчик понимал, как устроен бот и какие файлы трогать.**

### 1.1 Архитектура проекта

- [x] Создать `docs/architecture.md`:
  - [x] Кратко описать пайплайн: Telegram update → `dispatcher` → `handlers/*` → `repositories/*` → БД.
  - [x] Для основных разделов:
    - `/today`
    - Дом (`/home` / кнопка 🧹 Дом)
    - Деньги (`/money` / кнопка 💰)
    - Еда
    - /ask_mom
    - движение/тело
  - [x] Для каждого раздела указать:
    - какие команды/кнопки ведут сюда;
    - какие хендлеры задействованы (файлы/функции);
    - какие таблицы/модели используются.

- [ ] Обновить `README.md`, добавив раздел «Для Codex / ИИ-разработчика»:
  - [x] Ссылки на:
    - `docs/architecture.md`
    - UX-спеки (см. ниже)
    - актуальный `sprints/sprint3.md`
  - [x] Короткий чеклист: «Прежде чем что-то менять → прочитай README → architecture → sprint3».

### 1.2 UX-спеки

- [x] Создать `docs/ux_today.md`:
  - [x] Описать цель `/today`: главный дашборд дня, без портянок.
  - [x] Структура:
    - верхний блок: очки, прогресс по важным делам, финансы «до зарплаты»;
    - блок «Дом»: 1–3 ближайших дела + ссылка на подробный план;
    - блок напоминаний/рутин (если есть);
    - минимальный набор кнопок: «Напоминания», «План по дому», «Финансы», «Мои очки».
  - [x] Привести 2–3 примера сообщений `/today` для разных сценариев.

- [x] Создать `docs/ux_home.md`:
  - [x] Описать первый экран Дом:
    - текст в духе: «Дом: помогу навести порядок по-человечески…»;
    - кнопки: `🧽 Уборка сейчас`, `📅 План на неделю`, `📋 Все дела по дому`, `🧴 Запахи / стирка`.
  - [x] Спеки для:
    - «План на неделю»
    - «Все дела по дому»
    - «Уборка сейчас» (какие шаги, какие кнопки, как начисляются очки).
  - [x] Примеры коротких диалогов.

- [x] Создать `docs/ux_ask_mom.md`:
  - [x] Описать стартовый экран /ask_mom:
    - текст приглашения;
    - кнопки тем, НЕ дублирующие меню (стресс, стирка, кухня, уборка, деньги, переезд, здоровье и т.п.).
  - [x] Поведение при выборе темы:
    - карточка из `mom_tips`;
    - опции: «Сделать делом», «Сделать напоминанием», «Ещё совет».
  - [x] Поведение при свободном вопросе:
    - поиск по тегам;
    - fallback-карточка «если совсем не знаешь, с чего начать».

---

## 2. Дом и уборка: стабилизация + UX (продолжение 2.5)

**Задача: чтобы раздел «Дом» и «Уборка сейчас» реально можно было использовать в уборочный день, без падений и хаоса.**

### 2.1 UX раздела «Дом»

- [ ] Первый экран Дом:
  - [ ] Текст и кнопки привести к спеке из `docs/ux_home.md`.
  - [ ] Старые кнопки «График уборки», «Зона недели», «Регулярные дела», «План по дому», «Настроить план» спрятать за `📋 Все дела по дому` и/или удалить, если они полностью заменены.

### 2.2 Модель `home_task` и регулярка

- [ ] Привести модель регулярных дел по дому к единой структуре (если ещё не):
  - поля: `id`, `user_id`, `title`, `zone`, `freq_days`, `last_done`, `next_due`, `points`, `is_active`.
  - `zone` — один из: `bathroom`, `kitchen`, `bedroom`, `hallway`, `laundry`, `fridge`, `misc`.
  - `freq_days` — целое число; `last_done` и `next_due` — ISO DATE.
- [ ] Добавить/проверить helper для пересчёта `next_due`:
  - [ ] при выполнении (`last_done = today`);
  - [ ] при отложке (`+1/+3/+7` дней);
  - [ ] использовать один и тот же helper во всех хендлерах.
- [ ] Для новых пользователей:
  - [ ] При первом заходе в Дом создать дефолтный набор задач (еженедельно/раз в 2 недели/месяц/3 месяца — как в спринте 2.5).
  - [ ] Сохранить частоты так, чтобы их можно было поменять в UI.

### 2.3 «План на неделю» и «Все дела по дому»

- [ ] `📅 План на неделю`:
  - [ ] Выбирать `home_task` с `next_due` в ближайшие 7 дней.
  - [ ] Сортировать по дате и показывать компактный список (до N строк, остальное — по кнопке «Показать ещё», если нужно).
  - [ ] Для каждой задачи:
    - `✅ Сделано` → `last_done = today`, пересчёт `next_due`, начисление очков.
    - `↪️ Отложить` → диалог `+1 / +3 / +7` → пересчёт даты.
  - [ ] Не дублировать одну и ту же сводку после каждого нажатия.

- [ ] `📋 Все дела по дому`:
  - [ ] Список всех активных `home_task`: название, частота, `next_due` в формате `DD.MM`.
  - [ ] Кнопки:
    - `✅` — выполнить;
    - `⚙️ Частота` — выбор 7/14/30/90 или произвольное значение;
    - `🗑 Скрыть` — `is_active = false`, не показывать в списках.
  - [ ] После действия — перерисовать список.

### 2.4 «Уборка сейчас»

- [ ] Перепроверить реализацию:
  - [ ] Не падать из-за `sqlite3.Row` (всё приводить к dict перед работой).
  - [ ] Шаги уборки хранить в state, чтобы не терялись при редактировании/дропе сообщения.
- [ ] Разделить сценарии:
  - [ ] выбор «сил» (low/mid/high или аналог) и/или типа уборки (поверхностная/основная/глубокая);
  - [ ] под это — разные наборы шагов (3–7 коротких, конкретных действий).
- [ ] Для каждого шага:
  - [ ] кнопки `✅ Готово` и `⏭ Пропустить`;
  - [ ] начисление очков (микрошаги 1–2, большие 3–5).
- [ ] В конце:
  - [ ] итоговое сообщение: «Ты закрыла N шагов из M, +X очков, дома уже заметно легче.»;
  - [ ] кнопки: «Ещё круг» и «Назад в Дом».
  - [ ] не спамить повторной /today-сводкой.

---

## 3. Напоминания, рутины и календарь забот

**Задача: чтобы пользователь не терялся в /reminders и всё работало без падений.**

### 3.1 Свои напоминания

- [ ] Переделать UX добавления напоминания:
  - [ ] Ветку: «Добавить напоминание» → «О чём?» → «Когда?» (с пресетами: Сегодня/Завтра/Через час/Своё) → «Как часто?» (одноразово / раз в N дней / раз в неделю / по дню недели).
  - [ ] Меньше текста, больше кнопок.
- [ ] Список напоминаний:
  - [ ] Кнопка `⏰ Напоминания` в /today и/или меню:
    - список актуальных напоминаний;
    - кнопки «Добавить», «Изменить», «Удалить».
  - [ ] В /today показывать только ближайшие и не выполненные.

### 3.2 Календарь забот (здоровье/бумажки)

- [ ] Проверить, что:
  - [ ] хендлеры не требуют повторной регистрации;
  - [ ] при отметке используется DD.MM.YYYY;
  - [ ] напоминания создаются корректно (раз в N месяцев/лет).
- [ ] Тексты сделать короче и мягче («давай заглянем к зубному раз в год» и т.п.).

---

## 4. /today как дашборд: чистый и полезный

**Задача: чтобы /today давал чёткую картинку дня, без клавиатур-наростов и дублей.**

- [ ] Привести /today к спеке из `docs/ux_today.md`:
  - [ ] Верхний блок:
    - очки за сегодня и за неделю, стрик;
    - прогресс по важным задачам (ADHD-фильтр: максимум 3–5).
  - [ ] Блок Дом:
    - если есть актуальные дела: строка вида «🧹 Дом: сегодня 2 дела — Полотенца, Полы/пылесос.»;
    - если всё чисто: «🧹 Дом: на эту неделю по дому всё, можно отдохнуть.»
  - [ ] Блок Финансы:
    - строка «До зарплаты N дней, из бюджета на еду/быт осталось X ₽ (~Y ₽ в день).» или вариант, если бюджет выбрали.
  - [ ] Блок Напоминаний/рутин — по минимуму, без сильного повтора того, что уже в других разделах.

- [ ] Кнопки /today:
  - [ ] Оставить 3–4:
    - «Напоминания»
    - «План по дому»
    - «Финансы»
    - «Мои очки»
  - [ ] Починить callback «Мои очки» (stats) и других кнопок; убедиться, что ничего не падает.

- [ ] Убедиться, что:
  - [ ] нет дублей сообщений при отметке задач;
  - [ ] блоки, где нет данных, либо скрываются, либо пишут минимальную фразу.

---

## 5. Деньги: UX и стабильность

**Задача: чтобы раздел “Деньги” можно было использовать без знания /команд.**

- [ ] Экран Деньги:
  - [ ] Кнопки: «Записать трату», «Отчёт за неделю», «Лимиты», «Счета», «Советы».
  - [ ] Все действия доступны по кнопкам, а не только через /spent.

- [ ] Траты и лимиты:
  - [ ] Проверить, что `/spent` и естественный ввод («потратил 700 вкусвилл еда») работают через общий парсер (см. раздел 6).
  - [ ] /spent_week и /budget_cat не падают на `sqlite3.Row`, все rows приводятся к dict.
  - [ ] Формат сумм и дат человекочитаемый (без лишних технических деталей).

- [ ] Счета:
  - [ ] Экран «Счета» показывает список уже заведённых платежей, а не только «Не забудь оплатить…».
  - [ ] Возможность:
    - добавить счёт (название + сумма + периодичность/дата);
    - отметить как оплаченный (обновить дату);
    - показать в /today ближайшие 1–3 счета.

---

## 6. Тональность, тексты и «мамин» стиль

**Задача: чтобы HiDL везде звучала как одна и та же заботливая женщина, а не сборная солянка.**

- [ ] Вынести стандартные фразы в `utils/texts.py` (если не сделано):
  - [ ] ack-ответы (ок, сделала, помню и т.п.);
  - [ ] ошибки (мягкие, без ругани);
  - [ ] формулировки щадящего режима и стриков.
- [ ] Проверить, что:
  - [ ] везде используется женский род («я сделала», «я напомню»);
  - [ ] вызовы типа `tone_ack`, `tone_error`, `register_text` применены во всех основных хендлерах: Дом, Деньги, Напоминания, /today, /ask_mom.
- [ ] /ask_mom:
  - [ ] убедиться, что темы не дублируют Дом/Еду по структуре, а именно объясняют, «как мама бы объяснила»;
  - [ ] добавить 3–5 карточек по стиралке (значки, куда сыпать порошок, режимы);
  - [ ] добавить 3–5 карточек «если нет сил» (микро-шаги для тяжёлых дней).

---

## 7. Натуральные команды и подготовка к голосу

**Задача: один парсер для «человеческого текста», который потом можно будет кормить и расшифровкой голосовых.**

- [ ] Создать `docs/nl_commands.md`:
  - [ ] Описать типы фраз:
    - траты;
    - напоминания;
    - уборка («что по дому сделать сегодня»);
    - вопросы к маме.
  - [ ] Для каждого типа — 3–5 примеров.

- [ ] Выделить модуль `utils/nl_parser.py`:
  - [ ] Описать dataclass `ParsedCommand` (тип команды + аргументы).
  - [ ] Реализовать базовый парсер:
    - «потратил 700 вкусвилл еда» → тип `expense`, сумма 700, категория `еда`, метка `вкусвилл`;
    - «напомни завтра в 12:00 помыть полы» → тип `reminder`, дата/время, текст.
  - [ ] Использовать этот парсер:
    - в разделе Деньги (вместо расползания нескольких парсеров);
    - в напоминаниях;
    - в свободном вводе в /ask_mom (для определения, это вопрос к маме или явная команда).

- [ ] Подготовка к голосовым:
  - [ ] Создать `docs/voice_integration.md`, где честно зафиксировать:
    - Telegram премиум умеет делать voice→text на стороне клиента;
    - бот ожидает уже **текст** и обрабатывает его через `nl_parser`;
    - как именно сторонний сервис/клиент может встроиться (интерфейс тот же).

---

## 8. Чат-режим /talk: каркас под LLM

**Задача: ничего не ломать, но дать понятный контур для будущей болталки.**

- [ ] Создать `docs/talk_mode.md`:
  - [ ] Цель режима: тёплая переписка, где HiDL:
    - выслушивает;
    - даёт мягкие советы;
    - предлагает действия, но не выполняет их без явного подтверждения.
  - [ ] Примеры диалогов (2–3):
    - про бардак;
    - про деньги и тревогу;
    - про отсутствие сил.

- [ ] В коде:
  - [ ] Добавить базовый хендлер `/talk`:
    - принимает текст;
    - возвращает stub-ответ в стиле HiDL (можно на основе `text_utils`);
    - логирует диалог в файл/таблицу для анализа (без чувствительных данных).
  - [ ] Создать `llm_client.py` или аналог:
    - функции-обёртки (`ask_llm(prompt) -> str`), сейчас — заглушки/комментарии.

- [ ] В меню:
  - [ ] Добавить кнопку «Поговорить» (или похожее название), пока с сообщением «чат-режим в разработке».

---

## 9. Техдолг, логирование и дымовые тесты

**Задача: чтобы Codex не ломал всё при каждой итерации, а ты не ловил падающие кнопки руками.**

### 9.1 sqlite3.Row и dict

- [ ] Пройтись по основным хендлерам:
  - Дом (`handlers/home_tasks.py` или аналог);
  - Напоминания (`handlers/custom_reminders.py` и т.п.);
  - Деньги (`handlers/finance.py`);
  - Еда/гайды (`guides.py`, если там есть доступ к Row).
- [x] Везде, где используются `sqlite3.Row`, явно приводить к dict, прежде чем обращаться по ключам.
- [x] Добавить короткий helper `row_to_dict(row)` и использовать его.

### 9.2 Логирование и graceful fail

- [ ] Добавить простую систему логирования:
  - [ ] лог ошибок в отдельный файл или stdout с префиксом `[ERROR]`.
  - [ ] лог ключевых действий (дом, напоминания, деньги) — по минимуму.
- [ ] Центральный helper, который:
  - [ ] ловит исключения в критичных хендлерах;
  - [ ] пишет лог;
  - [ ] отвечает пользователю мягкой фразой без падения бота.

### 9.3 Smoke-тесты

- [ ] Создать `tests/smoke.py`:
  - [ ] попробовать импортировать основные модули (`handlers`, `repositories`, `finance`, `guides`, `utils`);
  - [ ] прочитать `mom_tips` (YAML/JSON) и проверить, что парсинг проходит без ошибок.
- [ ] Добавить в README команду для запуска дымового теста.
- [ ] В конце спринта:
  - [ ] прогон: `/start → /today → Дом → Уборка сейчас → План по дому → Напоминания → Деньги → Мои очки`;
  - [ ] убедиться, что ни одна кнопка не вызывает падение или повторную регистрацию.

---

## 10. Dev UX: запуск, .env, CONTRIBUTING

**Задача: чтобы проект можно было поднять с нуля без квеста, а Codex имел правила игры.**

- [ ] Добавить `.env.example`:
  - [ ] перечислить нужные переменные (токен бота, БД, режим DEV/PROD);
  - [ ] без реальных секретов.
- [ ] Добавить простой скрипт запуска:
  - [ ] `scripts/run_dev.sh` или аналог, который:
    - активирует venv (если нужно);
    - запускает бота;
    - пишет короткую подсказку в консоль.
- [ ] Создать `CONTRIBUTING.md`:
  - [ ] Чеклист до изменения:
    - прочитать `README`, `docs/architecture.md`, `sprints/sprint3.md`;
    - не вводить новые команды без UX-спек.
  - [ ] Чеклист после изменения:
    - запустить `tests/smoke.py`;
    - пробежаться по основному маршруту /start→/today→Дом→Деньги→Напоминания;
    - проверить, что даты и времена форматируются как DD.MM.YYYY и HH:MM.

---

## 11. Критерии приёмки спринта

- [ ] Ни одна из основных кнопок не роняет бота:
  - Дом, Уборка сейчас, План по дому, Напоминания, Деньги, Мои очки, /today.
- [ ] `/today` компактный, без дублей и крипто-кнопок.
- [ ] Раздел Дом даёт реальный, выполнимый план на неделю и «Уборку сейчас» с 3–7 шагами.
- [ ] Напоминания можно добавлять/просматривать/откладывать через кнопки, без знания /команд.
- [ ] Деньги: трату можно записать естественной фразой, отчёт и лимиты читаемы.
- [ ] HiDL везде говорит женским голосом, без стыда и «Нужно зарегистрироваться» в рандомных местах.
- [ ] Есть `docs/architecture.md`, UX-спеки и базовая документация по natural language и голосу.
- [ ] Smoke-тест проходит, `git status` чистый (без секретов/левых файлов).
