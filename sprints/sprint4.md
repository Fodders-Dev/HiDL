# Sprint 4 — День, поддержка и быт: планирование, аффирмации, витамины и кухня

Цель спринта:

* Дать пользователю ощущение, что HiDL не только пинает, но и поддерживает:
  * вечернее/утреннее планирование дня;
  * короткие «маминские» аффирмации/поддержку.
* Сделать первый внятный MVP по витаминам/таблеткам **без врачебных советов** — только напоминания и мягкая инфа.
* Продолжить развитие «Дом/Кухня» по результатам реальной уборки и готовки:
  * использовать идеи из `IDEAS/home_cleaning.md` и `IDEAS/kitchen_food.md`;
  * убрать оставшиеся UX-шероховатости.
* Дать пользователю больше контроля над рутинами (редактирование, зависимые шаги).
* Чуть упорядочить архитектуру и доки, чтобы Codexу было проще понимать, «куда лезть».

> Для Codex: как и раньше, отмечай выполненное `[x]`. Тексты — по-русски, от лица HiDL (женский род, тёплый тон, без шейминга).

---

## 1. Планирование дня (вечер + утро)

**Задача:** чтобы пользователь мог вечером накидать план на завтра, а утром — быстро его уточнить и утащить в `/today`.

### 1.1. Модель и хранение

- [x] Ввести сущность `day_plan`:
  - поля: `id`, `user_id`, `plan_date` (DATE), `created_at`, `updated_at`;
  - `items` — либо отдельная таблица `day_plan_items`, либо JSON:
    - `id`, `title`, `category` (`work`, `home`, `health`, `social`, `misc`), `is_important` (bool), `done` (bool).
- [x] Хранить **по одному плану на день** на пользователя (обновлять/переписывать при повторном планировании).

### 1.2. Вечернее планирование

- [x] Добавить команду/кнопку `План на завтра`:
  - доступ: из `/today` и из отдельной команды `/plan_tomorrow`.
- [x] Диалог (пример):

  1. «Давай придумаем завтрашний день.  
     Скажи, какие 1–3 важных дела точно хочешь успеть?» → свободный текст или кнопка «Пропустить».
  2. «Теперь давай чуть по жизни. Что-нибудь по дому/здоровью/для себя?» → можно выбрать из кнопок («Дом», «Здоровье», «Отдых», «Ничего»).
  3. В конце — краткий список:

     > Завтра для тебя главное:  
     > • …  
     > • …  
     > Остальное — бонус. Я напомню утром.

- [x] Сохранять план в `day_plan` (с разделением `is_important`).

### 1.3. Утренний чек и интеграция с /today

- [x] Добавить утренний пинг (при включённом режиме уведомлений и нормальном времени подъёма):
  - текст:

    > Доброе утро. Вчера ты запланировал(а) на сегодня:  
    > • …  
    > • …  
    > Что-то убрать/добавить?

  - кнопки:
    - `Всё ок`;
    - `Удалить пункт` → список;
    - `Добавить дело` → один текстовым шагом.
- [x] После подтверждения:
  - [x] перенести важные пункты в `/today` как «важные дела дня», пометить их в `day_plan` как «синхронизированы»;
  - [x] в `/today` в сводке добавить строку:

    > 🎯 План на день: N важных дел, M дополнительных.

- [x] При отметке выполнения этих дел в `/today`:
  - [x] проставлять `done = true` в `day_plan_items` (по id или заголовкам);
  - [x] учитывать их в очках (как обычные задачи/рутины).

---

## 2. Аффирмации и поддержка

**Задача:** дать HiDL маленький «цитатник/поддержку» в стиле приложений вроде I Am, но без эзотерики и кринжа.

### 2.1. База аффирмаций

- [x] Создать файл, например `data/affirmations.json` или `IDEAS/affirmations_seed.json`:
  - поля: `id`, `category` (`self_worth`, `anxiety`, `motivation`, `adhd`, `rest`), `text`, `tone` (`soft`, `cheerful`, `calm`).
- [x] Наполнить базу:
  - минимум 15–20 аффирмаций;
  - стилистика: простые, человеческие фразы, без магического мышления («всё обязательно получится» — ок, «вселенная всё устроит за тебя» — не надо).

Примеры (чисто для ориентировки, писать свои):

- «Ты имеешь право идти своим темпом. Даже маленький шаг — это движение.»
- «Дом не обязан быть идеальным, чтобы в нём было тепло и по-человечески комфортно.»

### 2.2. Команда и встраивание

- [x] Добавить команду `/affirm` (или кнопку «Поддержка» в wellness/пауза):
  - выдаёт одну случайную аффирмацию;
  - текст HiDL:

    > Давай я напомню тебе одну важную вещь на сегодня:  
    > _…аффирмация…_

  - кнопки:
    - `Ещё одну`;
    - `Спасибо, хватит`.
  - [x] Интегрировать аффирмации в:
  - [x] /pause или wellness: когда человек берёт паузу, предложить 1 короткую фразу поддержки.
  - [x] /stats — при хорошем стрике или наоборот при просадке (через `gentle_streak` можно иногда добавлять аффирмацию вместо сухого текста).

---

## 3. Витамины и таблетки (без диагностики и назначения)

**Важно:** HiDL **не врач**. Она только напоминает о таблетках, которые человек сам решил пить (или врач назначил), и даёт базовые подсказки, в каких продуктах бывают какие витамины.

### 3.1. Модель

- [x] Ввести таблицу `meds`:
  - `id`, `user_id`, `name` (строка, например «магний», «витамин D», «таблетки от давления»),
  - `dose_text` (человеческий текст: «1 таблетка», «5 капель»),
  - `schedule_type` (`once_daily`, `twice_daily`, `custom_times`, `days_of_week`),
  - `times` (list/JSON: например `["08:00", "21:00"]`),
  - `days_of_week` (список или null),
  - `notes` (про еду: «после завтрака», «строго после еды», «натощак»),
  - `active` (bool).
- [x] Таблица `med_logs`:
  - `id`, `user_id`, `med_id`, `taken_at` (datetime), `plan_date` (date), `planned_time` (HH:MM).

### 3.2. Настройка в боте

- [x] Раздел/подраздел «Витамины и таблетки»:
  - вход: из wellness/здоровья или из `/settings`.
- [x] Диалог добавления:
  1. «Как называется то, о чём тебе напоминать?» → текст.
  2. «Сколько и как часто ты это принимаешь?» → выбор:
     - 1 раз в день;
     - 2 раза в день;
     - Свой режим.
  3. Если 1–2 раза:
     - спросить времена (`08:00` / `21:00` через кнопки «утро/день/вечер» или ручной ввод времени).
  4. Если свой режим:
     - дать добавить 1–3 времени и, опционально, дни недели.
  5. В конце — резюме:

     > Ок, буду напоминать про «магний»: 1 таблетка, каждый день в 09:00, после завтрака.

### 3.3. Напоминания и интеграция в день

- [x] В общем механизме напоминаний:
  - добавить генерацию записей на каждый день по `meds` (типа «плановая точка»).
- [x] При срабатывании:
  - сообщение:

    > Пора «магний»: 1 таблетка, после еды.  
    > Ты уже принял(а)?

  - кнопки:
    - `Принял(а)` → лог в `med_logs`;
    - `Позже` → отложить на +30/60 минут.
- [x] В `/today`:
  - строка в сводке:

    > 💊 Таблетки: сегодня N напоминаний, уже выполнено M/N.

### 3.4. Инфо про витамины (без диагноза)

- [x] Добавить небольшой справочник `data/vitamins_info.json`:
  - `name` («витамин D», «витамин B12», «магний»),
  - `food_sources` — список продуктов;
  - `short_note` — 1–2 нейтральные фразы вида:
    - «Этот витамин часто обсуждают в контексте костей и иммунитета.  
       Конкретно тебе, чем и сколько — лучше спросить у врача.»
- [x] Команда `/vitamins_info` или кнопка «Про витамины»:
  - предлагает список популярных;
  - показывает, в каких продуктах встречаются;
  - **каждый раз** добавляет дисклеймер:

    > Я не врач. То, что организм «чувствует себя неважно», ещё не значит, что «не хватает витаминов».  
    > Если хочешь что-то принимать регулярно — лучше обсуди это с доктором.

---

## 4. Дом и уборка: доточка по `home_cleaning.md`

**Задача:** добить куски, которые обсуждали голосом и записали в `IDEAS/home_cleaning.md`, но ещё не зашиты в код.

### 4.1. «Параллельные» задачи при уборке

- [x] В сценарии «Уборка сейчас» добавить шаг **до сбора мусора**, если выбран режим «основная/глубокая уборка»:
  - предложить запустить «фоновые» процессы:
    - замочить раковину/ванну/унитаз (если есть средство);
    - залить средство на плиту/раковину на кухне;
    - поставить посуду замачиваться;
    - при наличии стиралки с грязным бельём — предложить запустить стирку.
- [x] Это всё должно быть оформлено как отдельный «подшаг»:
  - по кнопке `Готово` → просто переходим далее (ничего не логируем как `home_task`);
  - текст: «Супер, пусть всё это немного постоит, пока мы разбираемся с вещами».

### 4.2. Проветривание и «фон»

- [x] В начале сессии уборки (если не включён «я мёртв, делаем минимум»):
  - мягко предлагать:

    > Пока мы начнём убираться, можно открыть окно/форточку на 5–10 минут — воздух сам сделает часть работы.

- [x] Это предложение не должно быть навязчивым: одна кнопка `Открыла(открыл)` / `Пропустить`, без очков.

### 4.3. Спец-сценарии: ванна/туалет/кухня/полы

- [x] Добавить подрежимы `Тип уборки` внутри «Уборка сейчас»:
  - `Квартира в целом`;
  - `Только ванна/туалет`;
  - `Только кухня`;
  - `Только полы`.
- [x] Для каждого типа использовать шаблоны шагов из `home_cleaning.md`, например:
  - ванна: «намочить, посыпать/нанести средство, пройтись щёткой, оставить, потом смыть»;
  - кухня: плита + стол + раковина + духовка по отдельным шагам;
  - полы: «подмести/пройтись совком» → «протереть влажной тряпкой» → «особые уголки».
- [x] Под каждый тип — 3–7 конкретных шагов, как в IDEAS-описании.

### 4.4. Дополнительные лайфхаки в режиме уборки

- [x] После сценария посуды — напоминание:

  > Не забудь протереть вокруг раковины и кран — вода и капли потом превращаются в налёт.

- [x] После сценария плиты/кухни:
  - предложить проверить духовку, если она давно не чистилась (по `home_task` или по времени).

---

## 5. Кухня и еда: рецепты, остатки и сроки годности

Опираться на `IDEAS/kitchen_food.md`.

### 5.1. База продуктов на кухне

- [x] Ввести таблицу `pantry_items`:
  - `id`, `user_id`, `name`, `amount` (число), `unit` (`g`, `kg`, `ml`, `l`, `шт` и т.п.),
  - `expires_at` (DATE, nullable),
  - `category` (`крупы`, `мясо/рыба`, `овощи`, `фрукты`, `молочка`, `прочее`).
- [x] Команда/раздел «Что у меня есть»:
  - список текущих продуктов;
  - кнопки:
    - `Добавить продукт`;
    - `Изменить` (количество/срок);
    - `Удалить`.

### 5.2. Сроки годности и напоминания

- [x] Раз в день/два формировать список «подозрительных» продуктов:
  - `expires_at` в ближайшие 3–5 дней;
  - товары с уже прошедшей датой.
- [x] В `/today`:
  - строка вида:

    > 🧊 Продукты: у 2 вещей скоро истекает срок — творог, курица.

  - кнопки:
    - `Посмотреть` → список с датами;
    - `Придумать, что с этим приготовить` (пока можно просто отправлять в /ask_mom или /еда с подсказкой).

### 5.3. Рецепты: краткое описание → подтверждение → шаги

- [x] Для раздела «Еда»/рецептов:
  - изменить UX на двухшаговый:
    1. показать **краткую карточку**:
       - название блюда;
       - краткое описание;
       - список ингредиентов;
       - селектор «на сколько порций / приёмов еды».
    2. только после подтверждения пользовательской кнопкой `Готовим` — показывать пошаговый рецепт.
- [x] На основе количества порций:
  - пересчитывать ингредиенты;
  - если продукт есть в `pantry_items` — проверять, хватает ли:
    - если не хватает — писать:

      > У тебя гречки останется только 100 г — можешь добавить её в список покупок после готовки.

### 5.4. Списание продуктов после готовки

- [x] После подтверждения «Я приготовил(а)»:
  - уменьшать `amount` в `pantry_items` по использованным ингредиентам;
  - если `amount` → 0:
    - предлагать мягкое текстовое напоминание, что продукт закончился (покупки можно занести через финансы/напоминания).
- [x] Если `amount` остаётся, но сильно мало (порог, например `<= 1 порции`):
  - показывать:

    > Гречки осталось мало (100 г). В следующий раз её может не хватить — кинуть в список покупок?

### 5.5. Чеки (подготовка к будущему)

- [x] Добавить заглушку `/receipt_photo`:
  - принимает фото чека;
  - отвечает:

    > Я пока не умею распознавать чек, но план такой:  
    > буду вытаскивать из него продукты и добавлять в список «что есть дома».

- [x] сохранить метаданные в таблицу `receipt_photos` (id, user_id, created_at, file_id), чтобы потом можно было доехать до OCR.
- [x] В `IDEAS/` (или отдельном docs/) описать предполагаемый формат **распознанного** чека: список товаров, вес, цена.

---

## 6. Рутины и привычки: кастомизация и «шаги после шага»

**Задача:** дать пользователю возможность настроить утренние/дневные/вечерние рутины под себя и реализовать зависимые шаги типа «после завтрака — выпить таблетки».

### 6.1. Модель рутин

- [x] Ввести таблицу `routine_steps`:
  - `id`, `user_id`, `routine_type` (`morning`, `day`, `evening`),
  - `title`,
  - `order_index`,
  - `points` (1–3),
  - `is_active`,
  - `trigger_after_step_id` (nullable) — если нужно показывать только после другого шага.
- [x] При миграции:
  - создать дефолтные шаги по текущим рутинам (как они сейчас зашиты в коде).

### 6.2. Редактирование рутины

- [x] Добавить в настройках или в разделе «Рутины»:
  - список шагов для morning/day/evening:
    - показать в формате:
      > 1. Почистить зубы  
      > 2. Позавтракать  
      > 3. …  
  - кнопки:
    - `➕ Добавить шаг` (с выбором рутины);
    - `✏️ Переименовать`;
    - `👁 Скрыть/показать` (переключение `is_active`);
    - опционально `⇅ Поменять порядок` (через простейший механизм «поднять/опустить»).
- [x] Для каждого шага можно задать `trigger_after_step_id`:
  - UX: «Показывать этот шаг только после какого шага?» → список шагов рутины + вариант «Всегда».

### 6.3. Поведение в /today

- [x] При показе рутины:
  - выводить только активные шаги;
  - если у шага есть `trigger_after_step_id` — показывать его только когда «родительский» шаг отмечен как выполненный.
- [x] Пример:
  - шаг «Позавтракать»;
  - шаг «Выпить таблетки» с `trigger_after_step_id` = id «Позавтракать».
  - В /today:
    - сначала видим только «Позавтракать»;
    - после галки — появляется «Выпить таблетки».

---

## 7. Техдолг и удобство для Codex

**Задача:** чуть улучшить структуру и документацию, чтобы Codexу проще было понимать, какие файлы трогать для каких фич.

### 7.1. Короткий обзор архитектуры

- [x] Добавить `docs/architecture.md`:
  - кратко описать основные модули:
    - `handlers/*.py` — за что каждый отвечает (Дом, Финансы, Рутины, Напоминания, Wellness и т.д.);
    - `utils/*.py` — общие хелперы (даты, тексты, today-дашборд);
    - `data/*.json` — статические базы (лайфхаки, аффирмации, рецепты, витамины).
  - отдельный блок «Если хочешь поменять…»:
    - «…уборку» → `handlers/home_tasks.py` + `IDEAS/home_cleaning.md`;
    - «…рецепты» → `handlers/food.py` (или текущий модуль) + `IDEAS/kitchen_food.md`;
    - «…тон/тексты» → `utils/texts.py`.

### 7.2. Документация по IDEAS

- [x] В `README`:
  - добавить раздел «IDEAS/ — кладовка идей»:
    - `home_cleaning.md` — сценарии уборки;
    - `kitchen_food.md` — кухня/еда;
    - `future_vision.md` — долгосрочное видение.
  - описать, что спринты берут задачи отсюда.

### 7.3. Мини-рефакторинг под новые фичи

- [x] Проверить, что новые сущности (day_plan, meds, pantry_items, routine_steps) используют те же паттерны:
  - подключение к БД через общий модуль;
  - одинаковый формат дат/времени (ISO в БД, человекочит в UI через helper).
- [x] Добавить пару debug-логов в:
  - планирование дня;
  - витамины/таблетки;
  - рецепты/списания продуктов.
  - чтобы при падениях было видно, на каком шаге всё ломается.

---

## 8. Критерии приёмки спринта 4

- [x] Пользователь может:
  - вечером составить план на завтра и утром его увидеть/чуть подредактировать;
  - получить аффирмацию по запросу и иногда — мягкую поддержку внутри паузы/статистики;
  - завести хотя бы один «витамин/таблетку» и получать по нему напоминания, отмечать приём;
  - увидеть в `/today`, что с таблетками сегодня.
- [x] В Дом/Уборка:
  - «Уборка сейчас» предлагает параллельные шаги (замочки/фоновые процессы);
  - есть режимы «только ванна/кухня/полы»;
  - в сценарии посуды появляется напоминание протереть вокруг раковины.
- [x] В Кухне:
  - есть список «что есть дома»;
  - можно добавить продукт с количеством и сроком;
  - `/today` предупреждает о продуктах с истекающим сроком;
  - рецепт показывает краткую карточку → по кнопке даёт шаги;
  - после готовки продукты корректно уменьшаются.
- [x] Рутины:
  - можно добавлять/прятать шаги в morning/day/evening;
  - возможно настроить шаг, который появляется только после выполнения другого.
- [x] Документация:
  - `docs/architecture.md` существует и отражает текущую структуру;
  - `README` упоминает IDEAS/ и новые фичи.
