# Sprint 2.5 — Дом и уборка

Цель: чтобы сегодня можно было реально убраться с HiDL:

* понятный раздел «Дом»;
* внятный «план по дому» с датами;
* режим «уборка сейчас» с выбором типа уборки (поверхностная/обычная/глубже в одну зону) и уровня сил.;
* базовые лайфхаки и частоты уборки из библиотеки «Спросить маму».

> Для Codex: отмечай выполненное `[x]`. Тексты — по-русски, от лица HiDL (женский род, тёплый тон, без шейминга).

---

## 1. UX раздела «Дом»

* [x] Переделать первый экран «Дом» (кнопка 🧹 Дом в главном меню).
  * [x] Показать приветственный текст из примера (можно переформулировать, но по смыслу сохранить).
  * [x] Спрятать старые кнопки «График уборки», «Зона недели», «Регулярные дела», «План по дому», «Настроить план» за пунктом «📋 Все дела по дому».

  Текст (пример, можно чуть переформулировать):

  > Дом: помогу навести порядок по-человечески.
  > Выбирай, что сейчас актуальнее — быстренько убраться, посмотреть план на неделю или разобраться с регулярными делами.

  Кнопки:

  * `🧽 Уборка сейчас`
  * `📅 План на неделю`
  * `📋 Все дела по дому`
  * `🧴 Запахи / стирка`

  Старые кнопки типа «График уборки», «Зона недели», «Регулярные дела», «План по дому», «Настроить план» — спрятать за `📋 Все дела по дому` и/или объединить, чтобы не было простыни из 6–7 кнопок.

---

## 2. Модель регулярных дел по дому

> Если подобная структура уже есть — привести к ней и не плодить дублей.

* [x] Ввести (или добить) сущность `home_task` примерно с такими полями:
  * [x] Поля `id`, `user_id`, `title`, `zone`, `freq_days`, `last_done`, `next_due`, `points`.
  * [x] `zone` — `bathroom`, `kitchen`, `bedroom`, `hallway`, `laundry`, `fridge`, `misc`.
  * [x] `freq_days` хранить целым числом, `last_done`/`next_due` — ISO DATE.
  * [x] Пересчитывать `next_due` как `last_done + freq_days`, при пустом `last_done` — от даты создания/сегодня.
  * [x] Значение `points` по умолчанию 3–5, с возможностью менять.

  * `id`
  * `user_id`
  * `title` — короткое название: «Полотенца», «Постельное», «Полы/пылесос» и т.п.
  * `zone` — `bathroom`, `kitchen`, `bedroom`, `hallway`, `laundry`, `fridge`, `misc`
  * `freq_days` — 7, 14, 30, 90…
  * `last_done` — DATE, nullable
  * `next_due` — DATE, вычисляемое как `last_done + freq_days`; если `last_done` пусто — считать от даты создания или от сегодня
  * `points` — сколько очков давать за выполнение (по умолчанию 3–5)

* [x] Для нового пользователя при онбординге (или при первом заходе в Дом) создавать набор дефолтных задач с частотами:

  **Еженедельно (`freq_days = 7`):**

  * Полотенца в ванной (поменять и постирать).
  * Влажная уборка пола в основных комнатах.
  * Ванна/раковина/унитаз — быстрый цикл.
  * Проверка холодильника на просрок и завалы.
  * Стол/поверхности на кухне — протереть.

  **Раз в 2 недели (`freq_days = 14`):**

  * Смена постельного белья (простыни, наволочки, пододеяльник).
    (Реалистичный диапазон для быта — раз в 7–14 дней; пользователь сможет поправить частоту.)

  **Раз в месяц (`freq_days = 30`):**

  * Генерально помыть холодильник (полки, ящики, дверные уплотнители).
  * Прожиг стиралки горячей стиркой со средством от накипи/уксусом (без белья).
  * Пройтись по плинтусам/дверным ручкам/выключателям.
  * Влажная уборка в труднодоступных местах (под кроватью/диваном и т.п.).

  **Раз в 3 месяца (`freq_days = 90`):**

  * Почистить фильтр стиралки и, если актуально, фильтр пылесоса.
  * Разобрать аптечку и проверить сроки.
  * Разбор «хаос-угла» (стол/полка, где скапливается всё подряд).

* [x] Сделать helper, который пересчитывает `next_due` после выполнения/откладывания и использует ISO-даты в БД (если ещё не так).
  * [x] Поддержка смещения даты (+1/+3/+7) и пересчёт очков.
  * [x] Единый вызов после выполнения/откладывания в UI.

---

## 3. «План на неделю» и «Все дела по дому»

### 3.1. План на неделю

* [x] Кнопка `📅 План на неделю`:
  * [x] Подбор задач с `next_due` в 7 дней, сортировка по дате.
  * [x] Вывод краткого блока с датой и названием.
  * [x] Inline-кнопки `✅ Сделано` и `↪️ Отложить` для каждой задачи.
  * [x] Мини-диалог для `↪️ Отложить`: `+1 день`, `+3 дня`, `+7 дней`.

  * Берёт все `home_task`, у которых `next_due` попадает в ближайшие 7 дней.

  * Группирует по дням (или просто сортирует по дате).

  * Показывает краткий блок вида:

    > План по дому на ближайшую неделю:
    > • До чт 27.11 — Полотенца (ванна)
    > • До чт 27.11 — Полы/пылесос
    > • До пт 28.11 — Ванна/раковина/унитаз
    > …

  * Для каждой строки — inline-кнопки:

    * `✅ Сделано` — ставит `last_done = today`, пересчитывает `next_due`, даёт очки, перерисовывает план.
    * `↪️ Отложить` — открыть мини-диалог с вариантами `+1 день`, `+3 дня`, `+7 дней` (без сурового шейминга).

### 3.2. Все дела по дому

* [x] Кнопка `📋 Все дела по дому`:
  * [x] Список всех `home_task` с частотой и `next_due`.
  * [x] Кнопки: `✅` (выполнить), `⚙️ Частота` (пресеты 7/14/30/90 + ввод), `🗑 Скрыть` (soft delete).
  * [x] Перерисовка списка после действия.

  * Показывает полный список `home_task` с частотой и датой:

    > Все дела по дому:
    > • Полотенца — раз в 7 дней, следующее до 27.11
    > • Постельное — раз в 14 дней, следующее до 05.12
    > …

  * Кнопки на задачу:

    * `✅` — выполнить (как выше).
    * `⚙️ Частота` — выбрать из пресетов `7/14/30/90` или ввести своё число дней.
    * `🗑 Скрыть` — отключить задачу для этого пользователя (soft delete/флаг).

---

## 4. Режим «Уборка сейчас»

* [x] Кнопка `🧽 Уборка сейчас`:

  * [x] Шаг 1: спросить, ЧТО хочется сделать — тип уборки:
    * `✨ Быстрый порядок (поверхностная)`
    * `🧹 Нормальная уборка`
    * `🧽 Разобрать одну зону поглубже`

  * [x] Шаг 2: спросить, СКОЛЬКО сил:
    * `Почти не живая, давай минимум`
    * `Сил достаточно, могу нормально`
    * `Могу прям поработать`

  * [x] Шаг 3: на основе типа + сил собрать список из 3–7 конкретных шагов:

    * Для **поверхностной** уборки:
      * только лёгкие, визуально заметные задачи:
        * собрать одежду в одну корзину/стопку;
        * протереть стол/поверхность, куда чаще всего смотришь;
        * разгрузить раковину/замочить посуду;
        * вынести мусор, если ведро полное.

    * Для **нормальной** уборки:
      * 1–2 ближайших по сроку `home_task` (например, полотенца, полы, ванна);
      * плюс 1–3 быстрых шага из поверхностной уборки.

    * Для **глубокой уборки одной зоны**:
      * выбрать зону (`кухня`, `ванна`, `комната`, `коридор`) или взять зону с ближайшими задачами;
      * дать шаги вроде:
        * «разобрать одну полку/ящик»,
        * «протереть фасады шкафов»,
        * «пропылесосить/помыть под кроватью/диваном» и т.п.

    > Время (10/20/40 минут) можно считать внутренне для себя, но пользователю НЕ показывать цифры — он оперирует только типом уборки и уровнем сил.

  * [x] Для каждого шага выводить текст + inline-кнопки:
    * `✅ Готово`
    * `⏭ Пропустить`

  * [x] При `✅`:
    * отмечать шаг как выполненный;
    * начислять очки (1–2 за микро-шаг, 3–5 за «большую» задачу);
    * если шаг связан с `home_task` — обновлять `last_done`/`next_due` через общий helper.

  * [x] После завершения сценария показывать короткий отчёт:

    > Ты закрыла N из M шагов, +X очков.  
    > Дома уже заметно легче — можешь остановиться или сделать ещё один круг позже.


---

## 5. Интеграция с /today

* [x] В верхнем блоке `/today` уже есть очки и прогресс. Добавить туда краткую строку по дому:
  * [x] Если есть дела сегодня/завтра — показать 2–3 названия.
  * [x] Если чисто — вывод «можно отдохнуть».
  * [x] Добавить кнопку `📅 План по дому` → `📅 План на неделю`.

  * Если есть дела по дому с `next_due` сегодня/завтра:

    > 🧹 Дом: сегодня 2 дела по дому — Полотенца, Полы/пылесос.

  * Если всё чисто:

    > 🧹 Дом: на эту неделю по дому всё, можно отдохнуть.

* [x] В блоке «домашние дела» `/today` показывать **не больше 2–3** ближайших `home_task` + кнопку `📅 План по дому` → `📅 План на неделю`.

---

## 6. «Спросить маму» — лайфхаки по уборке и запахам

Цель: чтобы при теме «Уборка и порядок» и «Запахи» в /ask_mom не было воды, а были короткие, реально полезные советы, которые можно сразу сделать.

### 6.1. Структура карточки в mom_tips

* [x] Сделать/уточнить формат одной карточки (в `mom_tips.yaml` или `json`):

* `id` — строковый уникальный идентификатор.
* `category` — крупная категория (`"уборка"`, `"запахи"`, `"стирка"`, `"кухня"` и т.п.).
* `tags` — список тегов по ключевым словам.
* `title` — короткий заголовок.
* `question_example` — пример вопроса пользователя.
* `tips` — 2–5 очень конкретных пунктов.
* `can_create_reminder` — `true/false`.

Пример (как ориентир, не обязательно соблюдать именно YAML-синтаксис — важно содержание):

```yaml
- id: "room_quick_clean_10min"
  category: "уборка"
  tags: ["комната", "быстрая", "уборка", "10 минут"]
  title: "Быстрая уборка комнаты за 10 минут"
  question_example: "Как быстро прибраться в комнате, если всё раскидано?"
  tips:
    - "Собери всю одежду в одно место: корзина, стул или коробка. Не сортируй сейчас, просто убери с пола."
    - "Сваленную мелочь (чекы, бумажки, мелкие вещи) сложи в одну коробку/пакет — разберёшь отдельно, когда будет голова."
    - "Протри одну главную поверхность: стол, тумбу или комод. Остальное пока не трогай."
    - "Открой окно на 5–10 минут, если нет жёсткого мороза, и быстро проветри комнату."
  can_create_reminder: false
```

### 6.2. Карточки по уборке (уборка/запахи/кухня/стирка)

* [x] Перенести карточки в `mom_tips` в нужном формате.

#### 6.2.1. Уборка и порядок

**1. Быстрая уборка комнаты за 10 минут**

* `id`: `room_quick_clean_10min`
* `category`: `уборка`
* `tags`: `[комната, быстрая, уборка, 10 минут]`
* `title`: `Быстрая уборка комнаты за 10 минут`
* `question_example`: `Как быстро прибраться в комнате, если всё раскидано?`
* `tips`:

  1. Собери всю одежду в одно место: корзина, стул или коробка. Не сортируй сейчас, просто убери с пола.
  2. Мелкие вещи (бумажки, зарядки, ручки) сложи в одну коробку/ящик «разобрать позже», не расфасовывай по местам.
  3. Протри одну главную поверхность: стол, тумбу или комод — там, куда чаще всего падает взгляд.
  4. Быстро пройдись по полу: пылесос или влажная салфетка по самым грязным местам.
  5. В конце оглянись и просто похвали себя, даже если осталось «не идеально».

**2. Когда стыдно за бардак и не знаешь, с чего начать**

* `id`: `overwhelmed_where_to_start`
* `category`: `уборка`
* `tags`: `[бардак, стыдно, не знаю с чего начать]`
* `title`: `Если стыдно за бардак и непонятно, с чего начать`
* `question_example`: `Дома ужасный бардак, мне стыдно и я не знаю, за что взяться. Что делать?`
* `tips`:

  1. Выбери одну маленькую зону — стол, тумбочку или один угол. Убираем только её, остальное пока «заморожено».
  2. Поставь таймер на 10–15 минут и пообещай себе остановиться, даже если работа не закончена.
  3. Всё, что не знаешь куда деть, складывай в одну коробку/пакет «на потом», не зависай на решении судьбы каждой вещи.
  4. По окончании таймера остановись и зафиксируй: у тебя уже не «полный ужас», а одна понятная зона в порядке.
  5. Если появятся силы — сделаешь ещё один подход позже, но даже один такой круг — это уже прогресс.

**3. Как не утонуть в грязной посуде**

* `id`: `dishes_how_not_drown`
* `category`: `уборка`
* `tags`: `[посудa, раковина, кухня, лень]`
* `title`: `Как не утонуть в грязной посуде`
* `question_example`: `У меня постоянно гора посуды, не знаю, как с этим справиться.`
* `tips`:

  1. Договорись с собой: одна большая мойка в день — вечером или после основного приёма пищи.
  2. Если сил мало — сначала промой всё под струёй воды, чтобы еда не засыхала, а мытьё было проще.
  3. Мой по категориям: сначала стаканы и кружки, потом тарелки, в конце кастрюли/сковороды.
  4. Постарайся после приготовления еды сразу ополоснуть сковороду/кастрюлю — потом будет в разы проще.
  5. Если посуды совсем гора, включи музыку/подкаст и считай это «временем для себя», а не наказанием.

**4. Минимальный набор химии и тряпок для однушки**

* `id`: `minimal_cleaning_kit`
* `category`: `уборка`
* `tags`: `[набор, химия, тряпки, однушка]`
* `title`: `Минимальный набор для уборки в однушке`
* `question_example`: `Что вообще нужно купить для уборки, если у меня ничего нет?`
* `tips`:

  1. Универсальное средство для поверхностей (кухня/ванна/стол).
  2. Средство для унитаза и, по желанию, отдельное для ванной/раковины.
  3. Пара микрофибровых тряпок: одна для кухни, одна для ванной/остального.
  4. Щётка или губка для посуды + отдельная губка/щётка «для грязных мест» (ванна, раковина в ванной).
  5. Пакеты для мусора подходящего размера к твоему ведру.

#### 6.2.2. Запахи

**5. Запах из стиралки**

* `id`: `washing_machine_smell`
* `category`: `запахи`
* `tags`: `[стиралка, запах, плесень]`
* `title`: `Если от стиралки пахнет затхлостью`
* `question_example`: `От стиральной машины неприятно пахнет, что делать?`
* `tips`:

  1. После стирки всегда оставляй дверцу и лоток для порошка приоткрытыми, чтобы машинка просыхала.
  2. Протри резинку по кругу дверцы: там часто скапливается вода и грязь.
  3. Запусти горячую стирку (90 °C) без белья — с чистящим средством для стиралок или с небольшим количеством уксуса/лимонной кислоты.
  4. Проверь и прочисти фильтр (обычно внизу спереди): там может быть мусор, монетки, шерсть.
  5. Если запах не уходит, повтори горячую стирку через пару дней и понаблюдай, не остаётся ли вода в барабане.

**6. Запах из раковины/сифона**

* `id`: `sink_smell`
* `category`: `запахи`
* `tags`: `[раковина, запах, сифон, кухня]`
* `title`: `Если пахнет из раковины`
* `question_example`: `Из кухонной раковины неприятно пахнет, как исправить?`
* `tips`:

  1. Запусти в раковину горячую воду на пару минут — иногда этого уже хватает, чтобы смыть застой.
  2. Насыпь в слив немного пищевой соды, затем аккуратно налей уксус или просто очень горячую воду — это помогает разоружить жир и налёт.
  3. Не лей жир/масло прямо в раковину: вытирай сковороду салфеткой в мусор, затем уже мой.
  4. Если есть доступ к сифону — аккуратно разобрать, промыть и собрать обратно; если страшно — лучше позвать того, кому с этим комфортно.
  5. На будущее: периодически промывай слив горячей водой с небольшим количеством средства для посуды.

**7. Запах в холодильнике**

* `id`: `fridge_smell`
* `category`: `запахи`
* `tags`: `[холодильник, запах, еда]`
* `title`: `Если в холодильнике неприятный запах`
* `question_example`: `Холодильник пахнет странно, как быстро это исправить?`
* `tips`:

  1. Выкинь очевидные подозрительные продукты: всё с плесенью, странным цветом или давно забытое.
  2. Протри полки и ящики тёплой водой с каплей средства для посуды или содой, насухо вытри.
  3. Поставь на полку открытую миску с содой или половинку лимона — они подтянут остаточные запахи.
  4. Следи, чтобы контейнеры были закрыты, а рыба/лук/чеснок лежали в герметичных ёмкостях или пакетах.
  5. Раз в месяц делай мини-ревизию: выбросить явный «мёртвый груз» и протереть хотя бы одну полку.

**8. Квартира пропахла готовкой**

* `id`: `cooking_smell`
* `category`: `запахи`
* `tags`: `[готовка, запах, кухня, проветривание]`
* `title`: `Если квартира пропахла запахом готовки`
* `question_example`: `Наготовила, теперь вся квартира пахнет. Что сделать?`
* `tips`:

  1. Как только заканчиваешь готовить — открой окно на кухне и, если возможно, в соседней комнате, создай сквозняк на 5–10 минут.
  2. Включи вытяжку или хотя бы настольный вентилятор в сторону окна.
  3. Протри плиту и самые заляпанные поверхности — часть запаха идёт от жира и брызг.
  4. Не оставляй грязную посуду и сковородки с остатками еды до утра — сложи, замочи, самое пахучее промой сразу.
  5. Если запах держится, можно на плите ненадолго вскипятить воду с дольками лимона или чуточкой корицы (но не оставлять без присмотра).

**9. Как проветривать, если на улице холодно**

* `id`: `airing_while_cold`
* `category`: `запахи`
* `tags`: `[проветривание, холод, зима]`
* `title`: `Как проветривать квартиру, если на улице холодно`
* `question_example`: `На улице холодно, но дома душно и пахнет. Как проветривать?`
* `tips`:

  1. Вместо открытого окна на целый день лучше делать короткие «шоковые» проветривания по 5–10 минут.
  2. Открой окно настежь, по возможности сделай сквозняк (второе окно/дверь в другой комнате).
  3. Перед этим можно закрыть дверь в ту комнату, где холод совсем не терпишь (спальня), а проветрить кухню/зал.
  4. Проветривание 2–3 раза в день по 5–10 минут часто эффективнее, чем микропроветривание на час.
  5. Если совсем мёрзнешь — совмещай с моментами, когда активно двигаешься по дому (уборка, зарядка).

#### 6.2.3. Стирка и текстиль

**10. Как часто менять постельное бельё**

* `id`: `bedding_how_often`
* `category`: `стирка`
* `tags`: `[постельное, частота, стирка]`
* `title`: `Как часто менять постельное бельё`
* `question_example`: `Раз в сколько нужно менять постельное бельё?`
* `tips`:

  1. В среднем постельное бельё имеет смысл менять раз в 1–2 недели — смотри по самочувствию и жаре.
  2. Если сильно потеешь, спишь с питомцами или болеешь — лучше менять чаще.
  3. Не жди «идеального момента»: выбери любой удобный день недели и привяжи смену белья к нему.
  4. Храни чистый комплект так, чтобы его легко было достать — меньше шансов «забить».

**11. Как стирать полотенца, чтобы не воняли**

* `id`: `towels_smell`
* `category`: `стирка`
* `tags`: `[полотенца, ванна, стирка, запах]`
* `title`: `Как стирать полотенца, чтобы они не воняли`
* `question_example`: `Полотенца в ванной пахнут мокрой псиной, что делать?`
* `tips`:

  1. Не бросай полотенца в комок: после душа развешивай их ровно и максимально расправленными.
  2. Стирай полотенца примерно раз в неделю или чаще, если чувствуешь запах или влажность.
  3. Используй более горячий режим (60 °C, если ткань позволяет) и не перегружай стиралку.
  4. После стирки сразу достань полотенца и высуши до конца — сырые вещи быстро начинают пахнуть.

### 6.3. Поведение /ask_mom для уборки/запахов

* [x] При выборе тем «🧹 Уборка и порядок» и «Запахи» в /ask_mom:

  * выбирать случайную или подходящую по тегам карточку из `mom_tips`;
  * отвечать текстом `title + tips` в тёплом, поддерживающем тоне;
  * после ответа предлагать:

    * `✅ Сделать это делом` (добавить в /today один пункт);
    * `🔁 Напоминать об этом` (создать напоминание раз в N дней/недель);
    * `➕ Ещё совет по этой теме`.

* [x] При свободном вопросе (текстом):

  * парсить ключевые слова и искать ближайшую карточку по `tags`;
  * если точного попадания нет — отвечать карточкой «overwhelmed_where_to_start» и добавлять исходный вопрос в TODO/лог для будущего расширения базы.

---

## 7. Очки за уборку

Цель: чтобы уборка явно отражалась в геймификации, и было видно, что «я реально что-то сделал».

* [x] Для всех `home_task`:

  * обычные дела (еженедельные) — +3 очка;
  * ежемесячные/раз в 3 месяца — +5 очков;
  * значение `points` можно хранить в самой задаче и переопределять по желанию.

* [x] Для «🧽 Уборка сейчас»:

  * короткие шаги (типа «собрать одежду в одну кучу») — +1–2 очка;
  * крупные шаги (типа «пропылесосить комнату» или «помыть ванну») — +3 очка;
  * в конце сценария показывать: «Ты сделала N шагов, +X очков.»

* [x] В `/stats` добавить блок:

  > Дом: за последние 7 дней ты сделала N дел по дому и набрала X очков.
  > Комментарий от HiDL: короткая поддерживающая фраза, без шейминга —
  > «квартире уже заметно легче», «даже пара дел в неделю — это уже движение» и т.п.
